"use strict";

jQuery(document).ready(function ($) {
  // Initialize WordPress Color Picker into all HTML elements with the specified data-role
  $('[data-role="slope-color-picker"]').wpColorPicker();
});